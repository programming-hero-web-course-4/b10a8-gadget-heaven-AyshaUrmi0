

// eslint-disable-next-line react/prop-types, no-unused-vars
const CardCategory = ({eachCategory}) => {
    return (
        <div>
                Card
        </div>
    );
};

export default CardCategory;